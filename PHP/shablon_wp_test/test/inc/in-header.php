<?php
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	Заголовок окна, return string	//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

function get_test_title( ) {

	// Это ошибка?
	if ( is_404( ) ) { 

		echo 'Страница не найдена...';

	// Это категория?
	} elseif ( pull_test_data( get_query_var( 'category_name' ), true ) ) { 

		$category = get_category( pull_test_data( get_query_var( 'cat' ) ) );
		
		echo $category -> name;
		
	// Это станица или пост!
	} else {
	
		$post = get_post( );
		
		echo $post -> post_title;
	};
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	Лого сайта, return <a><img>	//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

function get_test_site_logo( ) {

	if ( function_exists( 'the_custom_logo' ) ) { the_custom_logo( ); }
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	Сайт инфо, return <h1>Title<p>Description	//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

function get_test_site_info( ) {

	//Название 
	if ( get_bloginfo( 'name' ) ) { 
		echo '<h1>' . get_bloginfo( 'name' ) . '</h1>'; 
	} 
	
	//Описание
	if ( get_bloginfo( 'description' ) ) { 
		echo '<p>' . get_bloginfo( 'description' ) . '</p>'; 
	} 
};

?>


